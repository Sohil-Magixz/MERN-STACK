1.HTML (HyperText Markup Language) is the standard language for creating web pages.
2.Emmet is a plugin for text editors that provides quick HTML and CSS coding shortcuts.
    html:5
    link:css
    div#id
    div.class
    a[href=""]
3.html:5
4.<!DOCTYPE html>
  <html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Document</title>
  </head>
  <body>
    
  </body>
  </html>
5.It contains meta-information about the html document, title, style, scripts, metadata.
6.contains the content of the HTML document that is visible to users, such as text, images, links, and other elements.
7. Elements are building blocks of html:
        <h1></h1>
        <p></p>
        <img>
    Tags:
        class
        style
        id



CSS
1.a language used for describing the presentation of a document written in HTML or XML, including aspects like layout, colors, fonts, and animations.
2.used to style and format web pages, enhancing their visual presentation and improving user experience by controlling layout, colors, fonts, and other design elements.
3.link:css
4.using google fonts, color can be given through 2 ways (Hex Codes) or color names.
5.text family: font-family,font-size,font-weight,font-style,text-align.
  color and background family: color, background-color, background-image, background-size, background-position,background-repeat
  box model:width,height,padding,margin,border
6. background to be used when we want contents to be displayed on top of the image, and IMG tag when only image needs to be displayed and no contents will be written on top.
7.refers to how elements are arranged on a page such as sizing,position,spacing. 
  flexbox properties:
        display:flex;
        flex-direction
        justify-content
        align-items
        flex-wrap
        flex.
8. tailwind css is a utility first css framework that provides  pre designed utility class.
